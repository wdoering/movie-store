describe("make sure subscribe to genre reducer", () => {
  it("should return true when genre/setGenreList is called", () => {
    // funny that the quickest way I found to a solution was using
    // integration tests and not unit testing
  });
});
